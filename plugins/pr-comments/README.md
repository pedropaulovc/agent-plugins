# pr-comments plugin

Provides the `/comments` skill: fetches active (unresolved) PR comments from GitHub, formats them for LLM consumption, and walks through reply drafting + thread resolution.

Pass `--include-resolved` to also include resolved threads.
