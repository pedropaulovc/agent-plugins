# worktree-reset plugin

Provides the `/m` skill: resets the current worktree branch to origin/main, cleans stale branches, removes untracked files (with confirmation), and runs `npm install` on all worktrees.
